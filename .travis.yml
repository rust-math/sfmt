language: rust

rust:
  - stable
  - beta
  - nightly

cache: cargo

script:
  - cargo test -v
  - cd sfmt-sys && cargo test -v && cd -
  - [[ "$TRAVIS_RUST_VERSION" -eq "nightly" ]] && cargo bench

addons:
  apt:
    packages:
      - gcc
